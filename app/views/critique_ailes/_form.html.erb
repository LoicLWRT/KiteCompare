<%= form_for(@critique_aile) do |f| %>
  <% if @critique_aile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@critique_aile.errors.count, "error") %> prohibited this critique_aile from being saved:</h2>

      <ul>
      <% @critique_aile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select(:type_critique_id, TypeCritique.find(:all).collect {|u| [u.name, u.id]}, :prompt => 'Select critique') %>
  </div>


  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>
  <div class="field">
    <%= f.label :lien %><br>
    <%= f.text_field :lien %>
  </div>
  <div class="field">
    <%= f.label :contenu %><br>
    <%= f.text_area :contenu %>
  </div>

  <div class="field">
    <%= f.select(:aile_id, Aile.find(:all).collect {|u| [u.modele.to_s + " " + u.annee.to_s, u.id]}, :prompt => 'Select aile') %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@materiel_type) do |f| %>
  <% if @materiel_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@materiel_type.errors.count, "error") %> prohibited this materiel_type from being saved:</h2>

      <ul>
      <% @materiel_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
